apiVersion: v1
kind: Service
metadata:
  name: be-svc
  namespace: be
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: be
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
