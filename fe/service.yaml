apiVersion: v1
kind: Service
metadata:
  name: fe-svc
  namespace: fe
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: fe
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
