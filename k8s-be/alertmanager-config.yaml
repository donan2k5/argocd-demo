apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: be-alert-email
  namespace: be
  labels:
    alertmanagerConfig: be
spec:
  route:
    receiver: email
    matchers:
      - name: alertname
        value: BeHighErrorRate
  receivers:
    - name: email
      emailConfigs:
        - to: tuphucnguyen20051@gmail.com
          from: alertmanager-demo@gmail.com
          smarthost: smtp.gmail.com:587
          authUsername: tuphucnguyen20051@gmail.com
          authPassword:
            name: alertmanager-email-secret
            key: password
          requireTLS: true
