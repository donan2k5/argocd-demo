apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: be
  namespace: be
  labels:
    app: be
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: be
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
